# 0K-35 Client Configuration
LUMI v1 selects one trusted, immutable ClientProfile per runtime instance. Profile schema v1 configures display identity, locale, allowed capabilities, neutral channels and knowledge enablement. It contains no credentials. Registered tools remain subject to runtime policy; a profile only reduces access. Concurrent multitenancy is out of scope.
