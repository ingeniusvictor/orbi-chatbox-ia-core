import { readFileSync } from "node:fs";import { DEFAULT_AI_PROVIDER } from "../src/config/aiProviderSelection.js";import { QWEN_LOCAL_RUNTIME_CONFIG } from "../src/config/qwenLocal.js";const a=(v:unknown)=>{if(!v)throw Error("Foundation close QA")};try{const r=readFileSync(new URL("../../POST_MVP_ROADMAP.md",import.meta.url),"utf8");a(r.includes("0K-23 — CLOSED")&&r.includes("0K-24 — Voice Interaction Foundation"));a(DEFAULT_AI_PROVIDER==="mock"&&QWEN_LOCAL_RUNTIME_CONFIG.model==="qwen3:1.7b"&&QWEN_LOCAL_RUNTIME_CONFIG.timeoutMs===30000);console.info("Post-MVP Foundation Close QA: PASS")}catch(e){console.error(e);process.exit(1)}
