import{ClientProfileRegistry,ORBI_DEFAULT_PROFILE,validateClientProfile}from"../src/config/clientProfile.js";const a=(x:unknown,m:string)=>{if(!x)throw Error(m)};try{const r=new ClientProfileRegistry();r.register(ORBI_DEFAULT_PROFILE);a(r.get("orbi-default").assistant.displayName==="LUMI","default");let bad=false;try{validateClientProfile({schemaVersion:2})}catch{bad=true}a(bad,"schema");let duplicate=false;try{r.register(ORBI_DEFAULT_PROFILE)}catch{duplicate=true}a(duplicate,"duplicate");console.info("Client Profile QA: PASS")}catch(e){console.error(e);process.exit(1)}
